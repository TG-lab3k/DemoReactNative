import {requireNativeComponent} from 'react-native';

/**
 * Composes `View`.
 */
//module.exports = requireNativeComponent('MagicTextView');

/**
 * Composes `View`.
 * - text: string
 */
export const MagicTextView = requireNativeComponent('MagicTextView');
